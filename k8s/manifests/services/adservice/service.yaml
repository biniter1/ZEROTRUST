---
apiVersion: v1
kind: Service
metadata:
  name: adservice
  namespace: production
  labels:
    app: adservice
    app.kubernetes.io/part-of: online-boutique
spec:
  type: ClusterIP
  selector:
    app: adservice
  ports:
    - name: grpc
      port: 9555
      targetPort: 9555
      protocol: TCP
